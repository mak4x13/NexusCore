# Combined deps for single-service Render deploy (backend + agents).
# Python 3.11+ required by band-sdk.

# Backend
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1
httpx==0.28.1
pyyaml==6.0.2

# Agents (Band SDK + LangGraph adapter + OpenAI-compatible client)
band-sdk[langgraph]
langchain-openai
