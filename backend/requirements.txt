fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1
httpx==0.28.1
pyyaml==6.0.2

# Band SDK (for the agents/ folder). Adapter extra picked per agent.
# band-sdk[langgraph]
# band-sdk[anthropic]
