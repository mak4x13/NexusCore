.env
agent_config.yaml
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
frontend/node_modules/
# Keep the built frontend in git because the Render Python service serves
# frontend/dist directly and does not run an npm build step.
